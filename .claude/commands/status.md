プロジェクトの現在の状態を一覧表示する。

手順:
1. `git status` で未コミット変更を確認
2. `git log --oneline -5` で直近コミットを表示
3. .company/secretary/notes/ の今日のprogress.mdがあれば末尾20行を表示
4. .company/secretary/notes/ の今日のdecisions.mdがあれば末尾10行を表示
5. 上記を整理して簡潔に報告
